<header id="users">
  <h1><%= pluralize(@users.count, "User") %></h1>
</header>
<ul class="user-list">
  <% @users.each do |user| %>
    <li><%= link_to user.name, user_path(user) %></li>
  <% end %>
</ul>